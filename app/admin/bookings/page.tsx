/** Admin — bookings confirm/decline + monthly export. Built W7 (design-doc §16). */
export default function AdminBookingsPage() {
  return (
    <div>
      <h1 className="font-display text-3xl text-brand-navy">Bookings</h1>
      <p className="mt-3 font-body text-brand-navy/60">
        Booking requests queue and monthly export — placeholder. Built in W7.
      </p>
    </div>
  );
}
